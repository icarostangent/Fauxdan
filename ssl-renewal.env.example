# SSL Renewal Configuration
# These variables control the automatic SSL certificate renewal

# SSL Renewal Interval (cron format)
# Default: Daily at 12:00 PM
# Examples:
#   "0 12 * * *"     - Daily at 12:00 PM
#   "0 0 * * 0"      - Weekly on Sunday at midnight
#   "0 0 1 * *"      - Monthly on the 1st at midnight
#   "0 */6 * * *"    - Every 6 hours
SSL_RENEWAL_INTERVAL=0 12 * * *

# Domain name for SSL certificates
DOMAIN_NAME=fauxdan.io

# Docker Compose project name (used for service discovery)
COMPOSE_PROJECT_NAME=fauxdan

# Other environment variables...
# DJANGO_SECRET_KEY=your_secret_key_here
# DJANGO_DB_NAME=your_db_name
# DJANGO_DB_USER=your_db_user
# DJANGO_DB_PASSWORD=your_db_password
# DJANGO_DB_HOST=your_db_host
# DJANGO_DB_PORT=your_db_port
